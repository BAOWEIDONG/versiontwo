# ─── 交付模板：复制本文件为 .env 后按需调整 ───
# Vite 构建期静默替换 import.meta.env.VITE_*；vite.config.ts 用 VITE_BASE 控制部署路径。
# 本文件不上传部署密钥，仅作为接入示例；实际使用请复制为 .env（被 .gitignore 忽略）

# 是否使用本地 mock 数据：
#   true  → 前端以本地 mock 数据独立演示，无需后端，可快速验收原型/演示
#   false → 发起真实 HTTP 请求到 VITE_API_BASE（生产接入后端时务必设 false）
VITE_USE_MOCK=true

# 后端服务根地址（仅 VITE_USE_MOCK=false 时生效）。
# 原型内所有业务接口均挂在 /api 前缀下（端点契约见 src/lib/api.ts 各方法注释）。
VITE_API_BASE=/api

# 部署基础路径（构建期生效）：
#   GitHub Pages 子路径：/versiontwo/
#   域名根目录部署：空 或 /
#   Nginx 子目录 /sub/：/sub/
#   改此项需重新 build
VITE_BASE=/versiontwo/